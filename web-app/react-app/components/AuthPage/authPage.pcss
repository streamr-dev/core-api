@import "@streamr/streamr-layout/pcss/variables.pcss";

.root {
  display: flex;
  flex-direction: column;
  justify-content: space-between;
  font-size: 16px;
  min-height: 100vh;
}

.content {
  background: inherit;
  padding: 0;
}

.panel {
}

.logo {
  padding: 4.25em 0;
}

.footer {
  /* … */
}
