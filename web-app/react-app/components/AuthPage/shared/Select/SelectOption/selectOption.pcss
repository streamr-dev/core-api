.root {
  align-items: center;
  display: flex;
}

.icon {
  flex-shrink: 0;
  padding: 0 1em;
  width: 2.625em;

  svg {
    width: 100%;
  }
}

.label {
  max-width: 100%;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}
